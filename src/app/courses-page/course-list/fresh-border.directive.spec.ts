import { FreshBorderDirective } from './fresh-border.directive';

describe('FreshBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new FreshBorderDirective();
    expect(directive).toBeTruthy();
  });
});
